$(document).ready(function() {

    //TURN BLOCKS RED//

    /*

    $(function() {
        var controller = new ScrollMagic.Controller();
        var blockTween = new TweenMax.to('.about-line', 1.5, {
            backgroundColor: 'red'
        });
        var containerScene = new ScrollMagic.Scene({
                triggerElement: '#one'
            })
            .setTween(blockTween)
            .addIndicators()
            .addTo(controller);
    });

	*/

	 //Pin Blocks//

	 /*

    $(function() {
        var pin = new ScrollMagic.Controller();
        var blockScene = new ScrollMagic.Scene({
            triggerElement: '#one'
        })
        .setPin('#one')
        .addIndicators()
        .addTo(pin);
    });

    */
});